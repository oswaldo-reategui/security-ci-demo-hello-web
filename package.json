{
  "name": "security-ci-demo-hello-web",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal Node/Express app with basic security CI (lint, tests, audit, dependabot).",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "lint": "eslint .",
    "test": "jest --ci --runInBand"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "eslint": "^9.37.0",
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
